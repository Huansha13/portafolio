export interface Home {
  id?:string;
  correo: string;
  descripcion: string;
  linkCv: string;
}

export interface Experiencia {
  icon: string;
  label: string;
  description: string;
}
